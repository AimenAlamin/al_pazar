import 'package:flutter/material.dart';

class PostingViewBody extends StatelessWidget {
  const PostingViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("body"),
    );
  }
}
